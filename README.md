# simple-WebRTC
simple webRTC 

# Cert and Key
you must be have cert.pem and key.pem 
and put in server/global/
    ``` httpsCert.pem ```
    ``` httpsKey.pem ```
# Open server 
``` sudo npm start ```   